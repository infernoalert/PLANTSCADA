Clean

Reads input/EQPARAM.csv and writes output/cleanEqparam.csv with every row whose
Equipment column contains your search text (case-insensitive). All columns from
the source are kept for those rows; rows that do not match are not included.

Enter a tag such as LU4107 in the top search box, then click Clean.
